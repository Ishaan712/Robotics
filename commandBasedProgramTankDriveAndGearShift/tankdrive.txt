package com.amhsrobotics;

import edu.wpi.first.wpilibj.GenericHID;
import edu.wpi.first.wpilibj.command.Command;

public class TankDriveCommand extends Command {

    private TankDriveCommand ourInstance = new TankDriveCommand();

    private TankDriveCommand getInstance(){

        return ourInstance;
    }

    public TankDriveCommand(){
        requires(DriveSubsystem.getInstance());
    }

    @Override
    protected void initialize(){

    }

    @Override
    protected void execute(){
        double goLeftSpeed = ExampleOI.getInstance().getController().getY(GenericHID.Hand.kLeft);
        //DriveSubsystem.getInstance().move(speed);
    }

    @Override
    protected void end(){

    }

    @Override
    protected void interrupted(){

    }

    @Override
    protected boolean isFinished() {
        return false;
    }
}